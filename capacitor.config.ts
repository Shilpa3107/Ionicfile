import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.system.design',
  appName: 'System_Design',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
